import { View, Text } from "react-native";
import React from "react";

const saved = () => {
  return (
    <View className="dark:bg-foreground bg-background flex-1">
      <Text>saved</Text>
    </View>
  );
};

export default saved;
